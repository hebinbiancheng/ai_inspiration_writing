# AI 小说创作工作台：技术选型与架构建议

> 状态：建议方案，待进入实现前确认  
> 日期：2026-08-20  
> 目标平台：Windows、macOS  
> 产品约束：本地优先、用户自带 API、核心客户端免费/优先开源、未来可选同步。

## 1. 结论摘要

建议采用：

| 层 | 选型 | 结论 |
|---|---|---|
| 桌面壳 | Tauri 2.x | 首选，负责窗口、本地文件、系统能力和打包 |
| 前端 | React 19.x + TypeScript + Vite | 负责页面、编辑器交互和界面状态 |
| 富文本编辑器 | Tiptap 3.x（基于 ProseMirror） | 所见即所得、可扩展、JSON 持久化 |
| 本地索引 | SQLite + FTS5 | 负责作品元数据、章节索引、全文搜索和派生数据 |
| 正文源文件 | 作品目录中的 Tiptap JSON | 保持可迁移，不把正文锁进数据库 |
| 本地核心 | Rust | 文件、SQLite、任务、模型请求、密钥和窗口能力 |
| Agent 编排 | TypeScript 定义角色与任务协议，Rust 负责可取消任务执行 | 第一版轻量编排，未来可扩展多 Agent |
| 模型适配 | 统一 `TextModelAdapter` / `ImageModelAdapter` 接口 | 首批 OpenAI 兼容 API、Ollama，以及可选图片 API |

## 2. 为什么首选 Tauri 2.x

### 2.1 与本地优先产品匹配

Tauri 官方提供窗口、本地能力、权限和跨平台打包能力；前端只通过明确的命令接口访问文件和系统能力。Tauri 的 capabilities/permissions 模型适合把“读写作品目录、访问密钥、调用模型”限制在清晰的接口内。

### 2.2 对透明窗口和桌面玻璃的支持

Tauri 官方文档明确支持透明窗口、窗口效果、无边框窗口和自定义标题栏。这样可以实现我们已经确认的三种窗口模式：标准窗口、桌面透明磨砂、高对比度窗口。

需要提前接受一个平台事实：Tauri 文档指出，macOS 透明窗口需要启用 `macos-private-api`；这可能影响 Mac App Store 审核。因此第一版建议发布签名/公证的 DMG 或 ZIP，不把 Mac App Store 作为首发渠道；如果未来必须上架 App Store，则改用透明标题栏和应用内部磨砂层，或对 macOS 使用平台特定实现。

来源：

- [Tauri Window Customization](https://v2.tauri.app/learn/window-customization/)
- [Tauri Window API](https://tauri.app/reference/javascript/api/namespacewindow/)

### 2.3 为什么不首选 Electron

Electron 也支持透明窗口，且生态成熟、纯 JavaScript 团队上手快。但本产品需要长期运行、处理超长作品、本地索引、文件和安全凭据；Tauri 的 Rust 核心更适合把这些本地能力集中在一个深模块里，同时避免把完整 Chromium 运行时作为应用基础负担。

Electron 仍是有效备选：如果团队缺少 Rust 能力，或编辑器/原生插件遇到 Tauri 特有阻碍，应重新评估 Electron，而不是为了体积目标牺牲交付速度。

来源：[Electron BrowserWindow](https://www.electronjs.org/docs/latest/api/browser-window)

## 3. 为什么使用 React + TypeScript + Vite

### React

页面包含首页、作品概览、三栏写作台、标签页、任务中心、资料确认、导入预览等大量交互状态。React 的组件、事件和状态模型适合把这些页面拆成可测试的界面模块；官方文档也把组件组合、事件和状态更新作为核心使用方式。

### TypeScript

本产品的领域对象较多：作品、卷、章节、人物、资料事实、Agent、任务、候选版本、审阅结果和附件。TypeScript 可以让这些对象在前端、Tauri 命令和模型适配器之间共享明确的数据契约，减少“AI 返回了另一种结构”这类问题。

### Vite

这是一个纯桌面本地应用，不需要服务端渲染。Vite 的开发服务器和生产构建适合将 React 前端作为 Tauri 的静态前端资源。不要使用 Create React App；React 官方安装文档已明确说明 CRA 已弃用。

来源：

- [React Quick Start](https://react.dev/learn)
- [React Installation](https://react.dev/learn/installation)

## 4. 为什么使用 Tiptap 3.x，而不是自行实现编辑器

用户要求开箱即用的所见即所得编辑器，支持标题、字体、段落、对话、图片、查找替换、版本差异和 AI 选区操作。Tiptap 基于 ProseMirror，提供节点/标记扩展模型、React 集成、选区操作、浮动菜单、字体/字号、查找替换、版本和快照相关扩展入口。

最重要的是，Tiptap 官方持久化文档明确建议优先保存 JSON：JSON 比 HTML 更灵活、更容易解析，也适合外部编辑。这个结论与我们已经确认的“结构化富文本 JSON 为主、HTML 为交换格式”完全一致。

建议内部只允许一个 `RichTextDocument` 接口：

```ts
type RichTextDocument = {
  schemaVersion: number;
  content: TiptapDocument;
};
```

Markdown、TXT、DOCX 都通过独立的导入/导出 Adapter 转换，不进入编辑器核心。

来源：[Tiptap Persistence](https://tiptap.dev/docs/editor/core-concepts/persistence)

## 5. 为什么使用 SQLite，但不把正文只存进 SQLite

SQLite 官方将其定义为 self-contained、serverless、zero-configuration 的嵌入式数据库，支持事务、单文件、跨平台文件格式和长期兼容，适合本地优先桌面应用。

建议职责分离：

- 作品目录中的 JSON：正文和正式作品资料的可迁移源文件。
- SQLite：作品列表、章节索引、FTS5 全文搜索、章节摘要索引、人物出场位置、伏笔位置、任务状态和缓存。
- 图片和附件：作品目录中的普通文件。

写入顺序：先写安全临时文件，再原子替换正文 JSON；随后更新 SQLite 索引。索引损坏时可从源文件重建，正文不会因为索引问题丢失。

来源：[About SQLite](https://www.sqlite.org/about.html)

## 6. 模块与深模块边界

采用“少量公共接口、复杂度藏在实现内”的深模块设计。

### `ProjectStore`

接口：打开、保存、快照、恢复、导入、导出、校验项目。  
实现：目录布局、原子写入、格式迁移、加密备份和损坏恢复。

### `RichTextEditor`

接口：加载 JSON、读取当前 JSON、执行编辑命令、读取选区、生成差异。  
实现：Tiptap/ProseMirror、工具栏、浮动菜单、图片节点、选区装饰和撤销栈。

### `SearchIndex`

接口：建立索引、全文搜索、按资料类型筛选、定位原文。  
实现：SQLite/FTS5、增量索引、人物出场和伏笔派生关系。

### `ModelGateway`

接口：以统一请求/流式事件/取消/错误模型调用文本或图片模型。  
实现：OpenAI 兼容 Adapter、Ollama Adapter、图片服务 Adapter、Token 统计和重试策略。

### `AgentRunner`

接口：运行一个已定义角色的任务，返回结构化候选、依据和用量。  
实现：上下文分层、提示词模板、模型选择、任务取消、候选持久化和未来多 Agent 顺序编排。

### `SecretStore`

接口：保存、读取、删除和测试模型凭据。  
实现：Windows Credential Manager、macOS Keychain；不把密钥写入作品目录。

### `TaskCenter`

接口：提交、排队、暂停、取消、重试和查询 AI 任务。  
实现：有限并发、当前写作优先、后台审阅、断点状态和通知。

## 7. 推荐目录结构

```text
ai-novel-workbench/
├── apps/desktop/                 # React + Tauri 壳
│   ├── src/                      # 页面和 UI
│   └── src-tauri/                # Rust 本地核心
├── packages/domain/              # 作品、资料、任务和 Agent 类型
├── packages/editor/              # Tiptap 扩展与富文本协议
├── packages/model-gateway/       # 模型适配器接口与前端协议
├── packages/import-export/       # TXT/MD/DOCX 转换器
├── packages/templates/           # 类型模板、Agent 模板和提示词
├── docs/                         # 产品规格、ADR、调研
└── prototype/                    # 当前一次性 UI 原型
```

## 8. 第一阶段实现顺序

1. 初始化 Tauri 2 + React + TypeScript + Vite。
2. 实现 `ProjectStore` 和作品目录格式。
3. 接入 Tiptap，完成舒适的富文本编辑和 JSON 保存。
4. 实现 SQLite 索引与本地全文搜索。
5. 实现 `SecretStore` 和模型配置页面。
6. 实现 `ModelGateway` 的 OpenAI 兼容 Adapter 与 Ollama Adapter。
7. 实现 `TaskCenter` 和 `AgentRunner` 的单 Agent 任务。
8. 接入新建作品、续写候选、审阅和批量导入流程。
9. 最后加入图片 Adapter、透明窗口开关和打包发布。

## 9. 最终建议

第一版采用 **Tauri 2.x + React 19.x + TypeScript + Vite + Tiptap 3.x + Rust + SQLite**。这是对当前产品约束的折中：

- 让作者获得成熟的所见即所得编辑体验；
- 让本地文件、密钥、索引和任务有可靠的原生实现；
- 让模型和 Agent 通过真实 Adapter seam 可替换；
- 让正文保持公开、可迁移；
- 为未来同步、图片模型和多 Agent 保留接口，但不提前引入复杂框架。
