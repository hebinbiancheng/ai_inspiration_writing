# AI 小说创作工作台：产品共创记录

> 状态：持续更新  
> 建立日期：2026-08-19  
> 本文档用于沉淀本次及后续对话中的需求、共识、分歧、决策和待办事项。

## 1. 项目缘起

用户希望构建一个 Web 工作台应用，使用 AI 帮助小说创作。目前处于产品探索阶段，需要先讨论清楚应用应当如何设计和实施，并将所有对话内容持续总结、保存为文档。

## 2. 当前目标

现阶段不急于直接进入编码，优先回答以下问题：

1. 产品首先服务哪一类小说创作者，以及他们最痛的创作环节是什么。
2. AI 在创作过程中扮演什么角色，哪些决定必须由作者掌控。
3. 工作台的核心工作流是什么，首个可用版本应包含哪些功能。
4. 如何组织人物、世界观、情节、章节和正文，使长篇创作保持一致。
5. 如何选择技术架构、模型能力、数据结构和交付节奏。

## 2.1 用户补充的产品定位

- 产品目标不是只服务单一作者，而是做成任何人下载后，配置自己的模型 API 即可使用的创作平台。
- 产品形态优先考虑本地可运行、配置简单、开箱即用；API 由用户自行提供，因此需要把模型接入设计成可替换能力。
- 主要服务网文连载和传统长篇；短篇、剧本和互动叙事纳入后续考虑，但不一定进入第一版核心流程。
- AI 的优先价值包括：灵感激发与大纲设计、续写，以及针对内容指出逻辑问题和文笔问题，帮助作者提升作品质量。
- 运行路线确定为“本地优先、未来可选同步”：第一阶段作品数据和主要工作流在本地完成，待体验验证后再考虑跨设备同步或云端备份。
- 第一版主流程确定为“灵感到成稿”：新建作品、发展灵感、形成故事方案、生成作品资料和大纲、选择章节续写、作者修改、AI 审阅。
- 后续需要支持从本地导入已有小说，并在已有内容基础上续写和审阅；该能力暂不作为第一版主流程，但数据模型需要预留。
- 模型接入优先支持 OpenAI 兼容 API 与 Ollama，以覆盖云端模型和本地模型。
- 桌面端优先支持 Windows 与 macOS。

## 3. 暂定产品愿景

打造一个面向小说作者的 AI 原生创作环境。它不只是“带聊天框的文本编辑器”，而应理解作品中的人物、世界观、时间线、情节约束和作者意图，在作者可控的前提下协助构思、规划、写作、修改和一致性检查。

这一定义仍是工作假设，需要通过后续讨论确认。

## 4. 初步问题空间

小说创作大致可能包含以下阶段：

- 灵感捕捉：记录题材、意象、人物火花和零散片段。
- 故事设计：确定主题、类型、核心冲突、人物弧光和结局方向。
- 世界构建：管理设定、地点、组织、规则、历史和术语。
- 情节规划：维护故事线、时间线、伏笔、章节目标和场景节奏。
- 正文创作：续写、改写、扩写、缩写、润色和风格控制。
- 编辑审校：检查人物行为、设定、时间、视角、节奏和前后矛盾。
- 版本与导出：比较修改、恢复版本，并导出常见文档格式。

这些不是已经确定的功能清单，而是用于识别核心工作流的探索范围。

## 5. 初步设计原则

- 作者拥有最终创作权；AI 提供建议、候选方案和可撤销的修改。
- 长篇上下文应结构化管理，不能只依赖不断增长的聊天记录。
- AI 生成内容应说明依据了哪些人物、设定或情节约束。
- 任何正文修改都应可比较、可接受、可拒绝、可恢复。
- 首个版本应围绕一个高频闭环做深，而不是堆叠大量孤立的 AI 按钮。

以上原则为初步建议，尚待用户确认。

## 6. 需要优先确认的问题

### 6.1 目标用户与写作场景

- 第一批用户是只服务用户本人，还是要做成面向其他作者的产品？
- 主要面向网文连载、传统长篇、短篇，还是剧本/互动叙事？
- 更偏中文写作，还是需要从第一版就支持多语言？

### 6.2 最核心的创作困难

- 没有灵感或难以开始。
- 大纲难以展开成章节和场景。
- 长篇写作容易遗忘设定、人物状态和伏笔。
- 正文表达、节奏或文风不够理想。
- 修改成本高，版本容易混乱。

需要选出最值得第一版解决的一个或两个问题。

### 6.3 AI 的角色与边界

- 顾问：提问、分析和给建议，不直接改正文。
- 合作者：根据指令生成候选文本，由作者选择。
- 代理编辑：在授权范围内执行批量修改，再由作者审阅。

产品可以最终支持多种角色，但首版需要明确主模式。

## 7. 潜在首版闭环（待选）

### 方案 A：设定驱动的章节写作

作者建立人物与世界观，制定章节目标，AI 依据约束生成场景草稿，作者在编辑器中修改，并将新事实回写到作品资料中。

### 方案 B：长篇一致性编辑器

作者导入已有作品，系统抽取人物、事件、时间线和伏笔，AI 发现矛盾并给出带依据的修改建议。

### 方案 C：灵感到大纲的故事策划室

作者从一个想法开始，通过结构化问答和方案对比，逐步得到人物、冲突、故事结构、章节大纲和写作计划。

三种方案可长期融合，但首版建议只选择一个作为产品主线。

结合用户补充，首版确定采用“故事策划 + 正文续写 + 内容审阅”的组合闭环，而不是只做单一生成器。三者以同一部作品资料作为共同上下文。

### 当前确定的 MVP 主流程

1. 新建作品并输入模糊灵感。
2. AI 通过追问和方案对比，协助形成故事方向。
3. 生成并编辑人物、世界观、核心冲突和章节大纲。
4. 进入指定章节，基于作品资料和上下文生成一个或多个续写候选。
5. 作者选择、修改或拒绝候选内容。
6. 对大纲或正文执行逻辑、设定、节奏、视角和文笔审阅。

### 后续扩展

- 从本地导入已有小说。
- 从已有正文抽取作品资料和大纲。
- 基于导入内容续写、改写和一致性审阅。

## 8. 风险与待验证假设

- 大模型处理长篇文本时可能遗漏早期信息或产生虚构事实。
- 过度自动生成可能削弱作者的声音和掌控感。
- 结构化资料与正文之间如果需要大量手工同步，会造成额外负担。
- 模型调用成本、响应时间和隐私要求会直接影响架构。
- 不同类型作者的工作流差异较大，过早做成“通用平台”可能缺乏清晰价值。

## 9. 对话摘要

### 2026-08-19：启动

- 用户提出构建一个使用 AI 帮助小说创作的 Web 工作台。
- 当前任务是共同探讨应用应该如何设计，而不是立即锁定实现方案。
- 用户要求把全部对话持续总结成文档并保存。
- 已建立本文档作为后续讨论的单一记录入口。

### 2026-08-19：产品定位补充

- 用户希望产品下载后配置 API 即可直接使用，面向广泛用户，而非仅为个人定制。
- 主要题材和形态优先级为：网文连载、传统长篇，其次考虑短篇、剧本和互动叙事。
- AI 首要帮助包括灵感与大纲、续写，以及逻辑和文笔问题诊断。
- 产品目标是辅助作者写出更好的小说，而不是完全替代作者。

## 10. 决策记录

### 已确认

1. 产品形态：下载即用、配置用户自己的 API。
2. 目标范围：面向广泛作者，优先支持网文连载和传统长篇。
3. AI 价值排序的第一版：灵感/大纲、续写、逻辑与文笔审阅。
4. 产品运行路线：本地优先，后续可选同步。
5. 第一版主流程：灵感到成稿，覆盖故事策划、续写和审阅。
6. 首批模型接入：OpenAI 兼容 API、Ollama。
7. 首批桌面平台：Windows、macOS。

### 尚未确认

- 支持哪些额外模型协议和 API 服务商。
- 第一版的核心用户旅程和功能边界。
- 作品资料库是否由系统自动维护，还是由作者主动维护。

## 11. 下一步

先确认目标用户、最核心痛点和 AI 的首要角色；随后据此定义首版用户旅程、信息架构和 MVP 范围。

## 12. 第一版信息架构建议（待确认）

### 12.1 工作台结构

- **作品首页**：最近作品、创建作品、导入入口、模型配置状态。
- **故事策划室**：灵感输入、AI 追问、故事方案对比、人物/世界观/冲突生成。
- **作品资料**：人物卡、世界观设定、地点与组织、时间线、伏笔、术语。
- **大纲**：卷、章、场景三级结构，以及每章目标和状态。
- **正文编辑器**：章节列表、所见即所得正文编辑、续写候选、接受/拒绝/重试。
- **AI 审阅**：逻辑、设定一致性、节奏、视角、文笔问题，以及定位到原文的建议。
- **设置**：API 配置、模型选择、数据目录、导入导出和备份。

### 12.2 推荐的主界面布局

左侧为作品与章节导航，中间为当前资料或正文编辑区，右侧为 AI 操作面板。AI 面板根据当前上下文提供策划、续写和审阅动作，避免让作者在多个页面间搬运文本。

## 13. 桌面技术方向建议（待确认）

建议采用跨平台桌面壳 + Web UI 的方式，优先评估 Tauri 或 Electron。核心要求是：Windows/macOS 打包稳定、本地文件访问可靠、可安全保存 API 配置、便于未来增加同步层。编辑器必须以所见即所得和低学习成本为优先，不能要求作者记忆 Markdown 语法或大量快捷键。

第一版应将模型接入抽象为统一接口，至少包含 OpenAI 兼容 API 和 Ollama 两个适配器；作品数据与模型服务解耦，未来可增加其他服务商而不改变作品结构。

这些是建议，不代表已经选定具体框架。

## 14. 对话摘要补充

### 编辑器方向修正

- 作者写作舒适性优先于 Markdown 语法和快捷键效率。
- 正文编辑器应采用所见即所得的富文本方式，常用格式、字体、标题、段落和对话排版通过工具栏、上下文菜单或明显的直接操作完成；快捷键只能作为可选增强。
- Markdown 不再作为作者的主要编辑界面，可保留为导出或交换格式；本地正文需要采用适合富文本编辑器且可迁移的存储格式。

### 编辑器体验修正

- 用户明确要求优先保证作者写作舒适性，正文编辑器不能依赖 Markdown 语法或要求记忆大量快捷键。
- 编辑器应采用开箱即用的所见即所得体验：标题、字体、段落、对话、引用、列表等常用格式通过可见工具栏、上下文菜单和直观操作完成。
- 快捷键可以作为熟练作者的增强能力，但不能成为完成基本写作任务的必要条件。
- Markdown 不再作为作者的主要编辑界面，可保留为导出或交换格式；底层正文应使用适合富文本编辑器且可迁移的格式。
- 编辑器视图确定为“沉浸写作视图 + 编辑排版视图”：两者共享同一正文数据，只改变界面呈现和工具密度。
- 正文底层格式确定为结构化富文本 JSON，HTML 作为兼容交换格式；不把 DOCX 或 Markdown 作为内部主格式。
- 第一版编辑格式范围确定为小说写作常用能力：段落、章节标题、粗体/斜体/下划线、字体字号颜色、对齐缩进、行距、引用、列表、分隔线、查找替换、字数统计和自动保存；暂不做复杂出版级排版。
- AI 审阅按范围区分：设定一致性、时间线和跨章节逻辑默认面向整部作品；当前章节可作为较快的局部审阅范围；选中文本主要用于表达、文笔和局部逻辑检查。所有审阅都允许作者手动选择范围。
- 长篇审阅确定采用分层上下文：作品资料层、章节摘要层、局部正文层；全书审阅可作为后台任务运行，完成后生成问题清单，不阻塞编辑器。
- 正文新增事实采用“自动发现 + 待确认更新”：可在保存章节、手动提取或审阅时触发；未经作者确认不得写入正式作品资料。
- 作品资料状态确定为：待确认、已确认、暂定、已废弃；状态影响 AI 续写和审阅时的约束强度，并保留变更历史。
- 第一版范围确定为单人本地创作，不做账号、实时协作和权限管理；未来同步成熟后再考虑共享作品。
- 本地可靠性确定为自动保存、关键操作自动快照、异常恢复、手动备份和导出副本；不未经授权上传作品，也不覆盖唯一副本。
- 删除策略确定为回收站、二次确认和版本恢复：普通删除可恢复，整部作品删除需确认路径，清空回收站才永久删除。
- 首次启动不强制配置 API：用户可直接新建、导入和编辑作品；AI 操作在需要时引导配置模型并测试连接。
- AI 成本控制确定为：调用前显示上下文范围、Token 或调用次数预估；调用后记录用量；大型任务需确认；支持输出长度、用量提醒和本地统计。
- 用户提出 AI 自定义需要与 Agent 角色体系结合：策划、续写、设定检查、逻辑审阅、文笔审阅等任务可能由不同角色承担，并考虑未来多 Agent 协作；普通写作偏好和高级提示词模板应建立在该角色体系之上。
- Agent 架构确定为内置轻量角色与任务编排：角色包含职责、上下文范围、工具、提示词和结构化输出；普通任务由单 Agent 完成，复杂任务可顺序组合多个 Agent；第一版不引入自主循环式多 Agent 框架，但保留扩展接口。
- 第一版 Agent 自定义范围确定为：可查看、调整和复制内置 Agent，可修改偏好、高级模板、默认模型和参数；从零创建、工具权限和自定义工作流留到后续。
- 多 Agent 冲突处理确定为：保留各角色独立意见与依据，由汇总角色解释分歧，作者最终裁决；不使用多数投票，不自动修改正文或正式资料。
- 小说类型模板方向已同意：模板可跳过、混合和修改，并影响追问、大纲、资料类型、Agent 偏好和审阅重点。用户进一步提出模板资料来源和治理问题，需要确保来源透明、版权合规和可维护。
- 模板来源治理确定为：官方模板基于公开叙事理论和人工整理的类型规律，经作者/编辑反馈迭代；AI 可辅助起草但必须人工审核；社区模板后续支持导入，每个模板记录作者、版本、许可证、来源和适用范围。
- 模板更新机制确定为：内置稳定版本离线可用；官方更新可选、可校验、显示变更并支持版本对比；不覆盖用户修改，社区模板默认不自动更新。
- 应用更新机制确定为：默认检查并提示但不强制安装；更新包需签名校验；项目格式迁移需确认且可回滚；更新失败不得影响现有作品。
- 遥测原则确定为默认关闭、用户主动开启、关闭不影响功能；不收集正文、提示词、模型回复、路径或密钥；崩溃报告需脱敏并允许预览。
- 性能基线确定为单部作品 500 万中文字、3000 章仍可正常编辑、浏览和搜索；按章加载正文，索引和审阅增量运行，AI 后台任务不得阻塞编辑器输入。
- 搜索导航范围确定为本地全文搜索替换、按卷章和资料类型筛选、结果上下文及原文定位、人物出场位置、伏笔埋设/回收位置和最近访问恢复；语义搜索留作增强能力。
- 编辑舒适性要求确定为浅色/深色/跟随系统、字体字号与页面宽度调节、高对比度、界面缩放、鼠标可完成核心操作和标准键盘导航。
- 动画策略确定为必要状态反馈保留、装饰性运动减少；支持跟随系统、完整动画和减少动画三种模式，减少动画时弱化位移、缩放和持续滚动。
- 校对机制确定为本地基础校对与 AI 深度审阅分层：本地检查错别字、重复、标点、括号、缩进等即时问题，支持自定义词典；AI 处理上下文表达和逻辑问题，均不自动修改正文。
- 写作统计确定为本地、可关闭，包含章节/全书字数、每日目标、连续天数和趋势；作者新增内容与接受的 AI 内容分开统计，不加入社交排行。
- AI 内容来源记录确定为后台保留 Agent、模型、操作、候选和后续修改；正文不做显眼标记，导出默认不附带内部记录，用户可查看或删除。
- 语言策略确定为第一版简体中文和中文创作体验优先，界面资源、模板和数据结构预留国际化；作品可输入其他语言，但第一版不承诺同等级校对质量。
- TXT 导入编码策略确定为自动识别 UTF-8、GBK/GB18030、UTF-16 等常见编码，不确定时允许逐文件切换并预览；不修改原文件，内部统一使用 Unicode。
- 附件方向确定为第一版支持作品封面、人物/地点/物品参考图片、地图和研究资料，并允许简单正文图片；附件复制到作品文件夹，避免原路径失效。
- 用户提出接入图片生成大模型 API；建议作为独立图像模型适配器，与文本 Agent 解耦，生成结果先预览并作为候选保存，记录模型、提示词和时间。
- 基础图片生成 API 接入纳入第一版，但作为可选模块；无图片 API 时封面、参考图和附件管理仍正常工作，角色一致性和复杂图像工作流留作后续。
- 文本模型与图片模型确定独立配置、独立适配和作品级选择；图片配置包含服务地址、密钥、模型、尺寸、质量和生成参数，图片调用失败不影响文本创作。
- 图片生成结果确定保留候选版本和生成记录：关联作品或资料卡，记录提示词、参数、模型和时间，由作者手动设为采用版本，默认不覆盖旧图。
- 作品格式策略确定为公开、可读、带版本号；应用升级自动迁移前先备份，迁移可回滚；公开 JSON Schema 和兼容包，未知字段不得静默删除。
- 第一版扩展范围确定为可导入的数据模板（Agent、类型、提示词、词典、校对规则和转换配置），暂不支持任意可执行插件；未来插件需权限、签名、沙箱和可撤销机制。
- 未来同步方向确定为同步层可替换，官方云服务只是一个实现；本地格式不依赖官方服务器，预留文件/对象变更记录、离线编辑、冲突检测、预览和手动合并能力。
- 未来同步冲突原则确定为冲突可见、默认不覆盖、作者决定；正文支持版本比较和合并，日志可追加合并，冲突解决前自动创建快照。
- 本地作品安全策略确定为默认可读、用户可选加密；加密项目使用密码或系统凭据，无后门恢复；API Key 与作品密钥分开管理，未来同步优先端到端加密。
- 加密备份确定为单文件加密备份包，包含作品及附件但不包含 API Key；恢复先校验完整性，默认创建副本；导出普通项目需提示会移除加密保护。
- 分发方式确定为 Windows 标准安装包 + 可选便携版，macOS 签名公证安装包；安装、卸载和升级不得删除作品数据，默认作品目录由用户选择。
- 文件交互确定支持项目和备份包文件关联、文件/文件夹拖拽导入、图片拖入资料卡或正文；文件关联不应自动覆盖作品。
- 项目损坏处理确定为打开前完整性检查、只读恢复、快照/正文重建和导出新项目；原项目不自动覆盖，诊断包默认不含正文。
- AI 可信度边界确定为：输出均为建议、候选或分析，不冒充事实；作者确认后才写入正式资料或正文；应用不保证质量、商业表现或版权归属，并明确云端数据传输提示。
- 模型失败处理确定为：错误分类可解释，原始指令和部分结果可恢复；部分结果只保存为未完成候选；支持重试、切换模型和取消，不无限重试、不伪造服务商拒答。
- Ollama 接入策略确定为只检测和引导：不自动安装或下载模型；读取本机/自定义地址的模型列表，下载需用户明确确认，服务不可用不影响本地功能。
- 第一版发布验收标准已确认：无账号本地启动；无 API 也可编辑；灵感到故事方案/资料/大纲；所见即所得写作；OpenAI 兼容 API/Ollama 续写；生成先预览；章节与全书审阅；批量导入与预览确认；自动保存、恢复、备份、导出；Windows/macOS 稳定；长篇性能和隐私要求达标。
- 首轮试用方案确定为 5～10 位作者，覆盖从零灵感、已有大纲、网文连载、批量导入、云端 API 和 Ollama；重点观察任务完成度、困惑点、创作收益、导入可靠性、编辑舒适性和安全担忧。
- 试用反馈机制确定为用户主动导出诊断包，导出前可预览；默认只含版本、系统、错误类型、性能摘要和脱敏崩溃信息，不含正文、提示词、回复、路径、密钥或图片；主观反馈另行收集。
- 首轮试用成功标准确定为三类：作者能独立完成核心任务；AI 能减少大纲/续写/审阅阻力并发现问题；作者信任数据边界、确认机制和编辑体验。不以生成字数或调用次数作为核心指标。
- 开发顺序确定为：本地项目与富文本编辑器 → 作品资料和大纲 → 文本模型适配 → 新建作品 AI 流程 → 续写候选确认 → AI 审阅 → 批量导入 → 导出/备份/恢复 → 基础图片生成。

### 原型阶段

- 已确认先制作交互原型，再开始正式开发。
- 视觉方向确定为磨砂玻璃质感、苹果式克制界面：半透明层、柔和背景光晕、清晰层级、充足留白和低干扰写作区域。
- 原型已创建于 [`prototype/novel-workbench.html`](./prototype/novel-workbench.html)，提供三个可切换方案：
  - A：三栏写作台，突出章节编辑与右侧 AI 协作。
  - B：新手入口，突出新建、导入、发展灵感和配置 AI。
  - C：作家仪表盘，突出进度、写作目标、待确认资料和灵感收集。
- 原型使用模拟数据，不接真实 API、不做持久化；通过 URL `?variant=A/B/C` 切换，也可使用左右方向键切换。
- 原型修复记录：Chrome 打开空白页的原因是脚本中声明了 `top()` 函数，与浏览器 `window.top` 的全局绑定冲突，导致 `Identifier 'top' has already been declared`。已改名为 `topbar()`，并用 Chrome headless 验证 A/B/C 三个变体都能渲染。
- 视觉修正记录：初版磨砂效果不明显，是因为面板背景过于接近不透明白色、背景光晕对比不足。已降低面板不透明度、增强蓝紫背景光晕、提高 `backdrop-filter` 的模糊和饱和度，并增加内高光与半透明边框；现在面板能明显透出背后的色彩层次。
- 窗口材质方向确定为用户可选：标准窗口、桌面透明磨砂，并保留高对比度/不透明模式作为可访问性选项。正式桌面应用由原生窗口层透出桌面背景，内容层继续使用半透明面板；当前浏览器原型只能模拟桌面背景和透明强度。
- 原型顶部已加入“标准窗口/桌面玻璃”切换按钮；桌面玻璃模拟模式也可通过 `?variant=A&glass=desktop` 直接打开。
- 原型页面关系确定为全部保留：B 作为应用首页，C 作为作品概览，A 作为章节写作台；核心导航路径为“应用首页 → 作品概览 → 章节写作”。
- 默认导航方向已确认：首次启动进入首页；打开作品进入概览；点击继续写或章节进入写作台；记住最后章节和光标，但不未经用户操作自动进入正文。
- 用户提出跨作品快速切换需求。建议在作品概览和写作台提供全局作品切换器：首页继续负责完整管理，切换器负责最近作品、搜索和快速跳转。
- 全局作品切换器已确认：顶部作品名称打开浮层，提供最近作品、搜索、新建、导入和全部作品入口；切换前自动保存并记住当前位置，默认进入目标作品概览；加密作品需解锁，后台任务继续运行。
- 同一作品内确定支持轻量标签页：可打开章节、人物卡、时间线和审阅结果，标签可关闭和固定，重启后恢复；默认单内容，标签过多收进更多菜单，第一版不做自由分屏。
- 三栏布局确定为左右侧栏可折叠、可调宽、可恢复默认；沉浸模式默认收起侧栏，窄窗口使用浮层，折叠 AI 面板时仍显示低干扰任务进度。
- 全局 AI 任务中心已确认：跨作品管理续写、审阅、资料抽取和图片生成，展示进度、模型、用量与状态，支持排队、暂停/取消、重试、切换模型和结果跳转；切换作品后任务继续运行。
- AI 任务并发策略确定为有限并发和可调整队列：云端默认 2 个，Ollama 默认 1 个，图片生成默认 1 个；当前写作任务优先于全书后台任务，限流后降低并发，不无限重试。

### 2026-08-19：MVP 与平台范围确认

- 第一版支持“灵感到成稿”的完整主流程。
- 后续增加本地已有小说导入、抽取资料、续写和审阅。
- 首批模型协议为 OpenAI 兼容 API 与 Ollama。
- 首批桌面平台为 Windows、macOS。
- 讨论过程中出现“Selected model is at capacity”提示；判断为当前对话模型服务的临时容量问题，与项目代码和文档无关。

### 2026-08-19：完整性访谈启动

- 用户要求使用 `grill-me` 对产品完整性进行系统追问，并持续形成文档。
- 访谈规则：一次只确认一个决策；每个问题同时给出建议；在用户确认共享理解前不进入实现。

### 完整性访谈：首要用户确认

- 第一版首要用户是有写作意愿、可能只有一个简单故事想法、希望将其发展为完整小说的中文作者。
- 这类用户通常需要从灵感发展到故事结构、章节和正文的连续辅助。
- “自然语言灵感输入 + AI 分阶段追问”仅用于新建作品；续写已有作品和导入本地小说不经过灵感追问流程。
- 产品入口应区分至少三种模式：新建作品、打开已有工作台作品、导入本地已有小说。
- 三入口已确认：打开已有作品用于恢复工作台项目；导入已有小说用于将外部文本转换为工作台项目，并在解析后由作者确认资料，不自动修改原文。
- 导入需要支持批量处理多个 TXT/Markdown/DOCX 文件，优先提供“选择文件夹”以及多选文件，而不是要求作者逐个导入。
- 批量导入应根据文件夹层级、文件名中的数字序号或自然排序推断卷/章顺序，并提供导入预览，让作者在生成作品前调整排序和章节边界。
- 批量导入过程中应保留原始文件内容，解析失败的文件单独提示，不应导致整部作品导入失败；重复导入需要有跳过、覆盖或创建副本的选择。
- 批量导入默认约定：一个被选择的文件夹代表一部作品，子文件夹代表卷，文件代表章节；散落文件按平级章节处理。
- 单个文件包含多章时，系统自动识别章节标题并在导入预览中允许合并、拆分、重命名和排序；无法识别时整文件作为一章。
- 导入后的 AI 抽取结果先进入“待确认资料”，作者可接受、修改、忽略或标记不确定；确认后才成为正式作品资料，并保留原文依据。
- AI 上下文优先级确定为：作者确认的作品资料 > 作者当前输入和章节目标 > AI 从正文推断的信息；发生冲突时必须明确提示作者。
- AI 生成内容默认不直接替换正文，必须先以候选、差异对比或插入预览呈现，由作者明确接受后写入；整章操作需生成版本快照。
- 版本管理采用轻量机制：关键修改前自动快照，支持版本列表、备注、差异对比和恢复；恢复前再保存当前版本。未采用的 AI 候选保留在创作记录中，不进入正文版本历史。
- 第一版 AI 审阅范围确定为五类：逻辑问题、设定一致性、时间线问题、情节与节奏、表达与文笔；结果必须定位原文并说明依据。暂不把“是否好看”或“是否商业爆款”作为客观判定。
- 隐私原则确定为：作品默认只保存在本地；API Key 使用 Windows 凭据管理器或 macOS Keychain；调用云端模型前明确告知；Ollama 默认访问本机；导出不包含密钥和敏感调用记录。
- 模型配置确定支持多个配置档案，可分别配置服务类型、Base URL、API Key、模型和生成参数，并按作品或具体操作选择；第一版不要求自动智能切换模型。
- 降级策略确定为 AI 可选、编辑功能永远可用：未配置或不可用时仍可新建、打开、导入和编辑作品；错误需具体可恢复，不能丢失草稿。
- 本地项目格式确定为“可移动作品文件夹 + 可迁移正文文件”：核心正文和作品资料应使用适合富文本编辑器的可迁移格式保存，应用可使用 SQLite 作为索引或缓存，但不把作品锁定在单一数据库中。Markdown 不再作为作者的主要编辑界面，仍可作为导出或交换格式。
- AI 记录确定归属于作品但与正文分离：保存指令、回复、模型、时间、操作类型和引用依据；候选放入 generations，审阅结果关联章节；后续任务按需引用，不默认拼接全部历史。
- 第一版导出范围确定为 Markdown、TXT、DOCX，支持整部或选定章节导出，并支持作品资料导出；暂不做出版级 PDF 排版和平台专用格式。
- 分发方向确定为：核心桌面应用免费，用户自行承担第三方模型 API 成本；优先考虑开源或至少公开作品数据格式；未来同步作为可选独立云服务。
- 开源范围方向确定为：本地桌面客户端、作品格式和模型适配层优先开源；未来同步服务可独立授权，不影响本地核心能力。

### 2026-08-20：技术栈建议（待用户确认）

- 桌面壳建议采用 Tauri 2.x；前端采用 React 19.x + TypeScript + Vite；富文本编辑器采用 Tiptap/ProseMirror；本地核心能力采用 Rust；SQLite 仅用于索引、搜索和可重建缓存。
- 选择依据：Tauri 适合本地优先应用，并提供原生文件、窗口、凭据和跨平台能力；React 适合复杂工作台的多面板状态管理；TypeScript 用于约束作品格式、模型适配器和 Agent 接口；Tiptap 支持 WYSIWYG 编辑及 JSON 持久化；Rust 将文件写入、密钥、任务取消等高风险能力收拢到稳定的本地边界；SQLite 适合零服务依赖的本地元数据和全文检索。
- AI 层不直接绑定某个 Agent 框架：先实现自有 AgentRunner + ModelGateway 接口，再接入 OpenAI 兼容 API、Ollama 和图片模型适配器；这样可以保留多 Agent 协作能力，同时避免第一版被第三方框架的状态模型和版本升级锁定。
- 透明窗口作为 Tauri 的可选窗口模式实现。macOS 的桌面透明效果可能涉及私有 API，因此第一版优先提供签名/公证 DMG 或 ZIP 分发，不把透明模式作为 Mac App Store 上架前提。
- 详细架构、目录结构、模块边界、实施顺序和替代方案记录在《技术选型与架构建议.md》中。

### 2026-08-20：技术栈正式采用

- 用户确认采用上述技术方案：Tauri 2.x + React + TypeScript + Vite + Tiptap/ProseMirror + Rust + SQLite。
- 已建立第一版工程骨架：`apps/desktop`、`apps/desktop/src-tauri`、`packages/domain`、`packages/model-gateway`，并加入可运行的三栏写作台前端示例。
- 前端依赖安装、TypeScript 类型检查和 Vite 生产构建均已通过；当前环境没有 Cargo/Rust，因此原生 Tauri 编译待安装 Rust 工具链后验证。
- 后续已安装 Rust stable、Tauri CLI 和 Windows 应用图标；Tauri Rust 核心 `cargo check` 已通过。
- 已实现第一版 `ProjectStore` 命令：创建作品目录、原子保存正文 JSON、读取作品清单元数据；前端“创建快照”按钮已接入该本地命令，浏览器预览模式会安全降级为未连接本地核心。
- 已接入 Tiptap/ProseMirror 编辑器，正文区域从静态示例升级为可编辑富文本；前端依赖、类型检查和生产构建均再次通过。当前构建仅提示编辑器包体积偏大，后续再做代码分割。
- 已接入 Tauri Dialog 文件夹选择器：左侧作品列表的“＋”可以选择真实作品目录，并调用 `ProjectStore` 初始化 `manifest.json`、正文、资料、候选和附件目录；Rust `cargo check` 已通过。
- 后续已安装 Rust stable、Tauri CLI，并完成 Tauri Rust 核心的 `cargo check`。
- 已实现 `ProjectStore` 初版：创建作品目录、写入 manifest、保存正文 JSON、读取 manifest；正文保存采用临时文件后原子替换，并拒绝越出作品目录的相对路径。

### 2026-08-20：技术栈确认与骨架启动

- 用户确认采用 Tauri 2.x + React + TypeScript + Vite + Tiptap + Rust + SQLite 的技术方案。
- 已建立 `apps/desktop` 前端应用骨架、`src-tauri` Rust/Tauri 配置、`packages/domain` 领域类型和 `packages/model-gateway` 模型接口。
- 已完成前端依赖安装、TypeScript 类型检查和 Vite 生产构建；当前环境未安装 Cargo，因此原生 Tauri 编译尚未执行。
